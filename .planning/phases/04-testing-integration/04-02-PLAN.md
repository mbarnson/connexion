---
phase: 04-testing-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/fixtures/openapi31_kitchen_sink/openapi.yaml
  - tests/fakeapi/openapi31_handlers.py
  - tests/openapi31/test_integration.py
  - tests/openapi31/conftest.py
autonomous: true

must_haves:
  truths:
    - "Kitchen-sink YAML spec loads as a valid OAS 3.1 spec through connexion"
    - "HTTP round-trip tests validate type arrays (string, null, invalid)"
    - "HTTP round-trip tests validate const keyword (valid and invalid values)"
    - "HTTP round-trip tests validate exclusiveMinimum as numeric"
    - "HTTP round-trip tests validate $ref with sibling preservation"
    - "HTTP round-trip tests validate allOf/anyOf/oneOf form data composition"
    - "Webhook parsing is verified (accessible, correct structure)"
    - "pathItems in components are referenced and routed"
    - "mutualTLS security scheme is recognized"
    - "jsonSchemaDialect top-level key is present and correct"
    - "Minimal document validity is confirmed (spec loads without paths)"
    - "nullable: true in 3.1 spec is rejected with actionable error"
    - "All existing 877+ tests still pass (zero regression)"
  artifacts:
    - path: "tests/fixtures/openapi31_kitchen_sink/openapi.yaml"
      provides: "Comprehensive OAS 3.1 spec exercising every supported feature"
      min_lines: 150
    - path: "tests/fakeapi/openapi31_handlers.py"
      provides: "Python handlers for kitchen-sink spec endpoints"
      min_lines: 30
    - path: "tests/openapi31/test_integration.py"
      provides: "Integration test with HTTP round-trips for all 3.1 features"
      min_lines: 150
    - path: "tests/openapi31/conftest.py"
      provides: "Fixtures for kitchen-sink app creation"
      min_lines: 10
  key_links:
    - from: "tests/openapi31/conftest.py"
      to: "tests/fixtures/openapi31_kitchen_sink/openapi.yaml"
      via: "build_app_from_fixture('openapi31_kitchen_sink')"
      pattern: "build_app_from_fixture"
    - from: "tests/fixtures/openapi31_kitchen_sink/openapi.yaml"
      to: "tests/fakeapi/openapi31_handlers.py"
      via: "operationId references"
      pattern: "fakeapi\\.openapi31_handlers\\."
    - from: "tests/openapi31/test_integration.py"
      to: "tests/openapi31/conftest.py"
      via: "kitchen_sink_app fixture"
      pattern: "kitchen_sink_app"
---

<objective>
Create a comprehensive OAS 3.1 kitchen-sink integration test with YAML fixture, fakeapi handlers, and HTTP round-trip tests covering every 3.1 feature connexion supports.

Purpose: Satisfy TEST-04 (every 3.1 feature has dedicated test coverage), TEST-05 (allOf/anyOf/oneOf form data tests), and TEST-07 (integration test with all features combined). This is the centerpiece of Phase 4 -- proving that all OAS 3.1 features work correctly through actual HTTP request/response cycles, not just load/validate.

Output: Kitchen-sink YAML fixture, fakeapi handlers, conftest with app fixture, integration test file with 15+ test functions covering all features.
</objective>

<execution_context>
@/Users/patbarnson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patbarnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-integration/04-RESEARCH.md
@.planning/phases/04-testing-integration/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kitchen-sink YAML fixture and fakeapi handlers</name>
  <files>tests/fixtures/openapi31_kitchen_sink/openapi.yaml, tests/fakeapi/openapi31_handlers.py</files>
  <action>
Create the comprehensive OAS 3.1 kitchen-sink spec and its Python handlers. The spec MUST exercise EVERY 3.1 feature connexion supports, with actual endpoints for HTTP round-trip testing.

**Kitchen-sink spec (`tests/fixtures/openapi31_kitchen_sink/openapi.yaml`) must include:**

1. `openapi: 3.1.0` with `jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema`
2. `info` with title and version (minimal valid document)
3. `paths` with endpoints exercising:
   - **Type arrays**: Endpoint accepting `type: ["string", "null"]` field -- test null, string, and invalid (integer) values
   - **const keyword**: Endpoint returning or accepting a `const` value -- test valid and invalid match
   - **exclusiveMinimum/Maximum as numeric**: Endpoint accepting integer with `exclusiveMinimum: 0` -- test boundary values (0 fails, 1 passes)
   - **$ref with siblings**: Schema using `$ref` alongside `description` and `summary` -- verify siblings preserved
   - **allOf composition**: Endpoint accepting body validated with allOf schema
   - **anyOf composition**: Endpoint accepting body validated with anyOf schema
   - **oneOf composition**: Endpoint accepting body validated with oneOf schema
   - **Form data with composition**: Endpoint accepting form data with anyOf schema (TEST-05)
   - **pathItems $ref**: Path that references `components/pathItems` via `$ref`
   - **mutualTLS secured endpoint**: Path with `security: [{mutualTLS: []}]`
   - **nullable rejection test**: Schema with `nullable: true` (should fail to load -- test this separately)
4. `webhooks` with at least one webhook operation
5. `components` with:
   - `schemas` using type arrays, const, $ref with siblings, exclusiveMinimum
   - `pathItems` for $ref from paths
   - `securitySchemes` with `mutualTLS` type
6. All operationIds point to `fakeapi.openapi31_handlers.<function_name>`

**Handlers (`tests/fakeapi/openapi31_handlers.py`) must include:**

Functions matching every operationId in the kitchen-sink spec. Keep handlers simple -- they receive request data and return appropriate responses. Each handler should return JSON that makes the test assertion possible (e.g., echo back received values).

Example patterns:
```python
def create_item(body):
    """Echo back the body for validation testing."""
    return body, 200

def get_version():
    """Return const version."""
    return {"version": "v1.0.0"}, 200

def health_check():
    """Health check for pathItem ref testing."""
    return {"status": "healthy"}, 200
```

Run `poetry run flake8 tests/fakeapi/openapi31_handlers.py` -- zero warnings.
  </action>
  <verify>
`ls tests/fixtures/openapi31_kitchen_sink/openapi.yaml` -- file exists
`ls tests/fakeapi/openapi31_handlers.py` -- file exists
`poetry run flake8 tests/fakeapi/openapi31_handlers.py` -- zero output
Kitchen-sink YAML is valid YAML (no syntax errors)
  </verify>
  <done>Kitchen-sink YAML spec covers all 3.1 features (type arrays, const, webhooks, pathItems, $ref siblings, exclusiveMin/Max, mutualTLS, jsonSchemaDialect, allOf/anyOf/oneOf, form data composition). Handlers provide responses for every endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test with HTTP round-trips</name>
  <files>tests/openapi31/conftest.py, tests/openapi31/test_integration.py</files>
  <action>
Create the integration test file and conftest fixture that exercises every kitchen-sink endpoint through HTTP round-trips.

**Conftest (`tests/openapi31/conftest.py`):**
```python
import pytest
from conftest import build_app_from_fixture

@pytest.fixture(scope="session")
def kitchen_sink_app(app_class):
    """Load the OAS 3.1 kitchen-sink spec through connexion."""
    return build_app_from_fixture(
        "openapi31_kitchen_sink",
        app_class=app_class,
        spec_file="openapi.yaml",
        validate_responses=True,
    )
```

**Integration test (`tests/openapi31/test_integration.py`):**

Must be a class-based test file `TestKitchenSinkIntegration` using the pattern from tests/api/*.py.

Required test coverage (each is a test function with actual HTTP calls via `app.test_client()`):

1. **test_spec_loads_successfully** -- Kitchen-sink spec loads without errors (fixture exists)
2. **test_type_array_accepts_null** -- POST with null value in type: ["string", "null"] field returns 200
3. **test_type_array_accepts_string** -- POST with string value returns 200
4. **test_type_array_rejects_invalid** -- POST with integer in string|null field returns 400
5. **test_const_valid_value** -- Validates that const matches
6. **test_const_invalid_value** -- Validates that wrong const value returns 400
7. **test_exclusive_minimum_rejects_boundary** -- POST with value == exclusiveMinimum returns 400
8. **test_exclusive_minimum_accepts_above** -- POST with value > exclusiveMinimum returns 200
9. **test_webhooks_parsed** -- Access spec.webhooks, verify webhook name and method exist
10. **test_path_items_ref_works** -- GET endpoint defined via pathItems $ref returns 200
11. **test_mutual_tls_recognized** -- Verify mutualTLS is in securitySchemes
12. **test_ref_siblings_preserved** -- Access spec components, verify $ref has description/summary siblings
13. **test_json_schema_dialect_present** -- Verify json_schema_dialect property returns correct value
14. **test_allof_composition_valid** -- POST body matching allOf returns 200
15. **test_anyof_composition_valid** -- POST body matching anyOf returns 200
16. **test_oneof_composition_valid** -- POST body matching exactly one oneOf schema returns 200
17. **test_form_data_anyof** -- POST form data matching anyOf composition validates correctly (TEST-05)
18. **test_nullable_rejection_in_31** -- Separate test: Building a spec with `nullable: true` in a 3.1 context raises InvalidSpecification or similar error

Tests that access the spec object directly (webhooks, mutualTLS, $ref siblings, jsonSchemaDialect) should access via `kitchen_sink_app.app.extra["apis"][0]` or similar pattern -- check how existing tests access the loaded specification.

Important: Use `with kitchen_sink_app.app.test_client() as client:` pattern from existing tests. Follow synchronous test patterns (avoid async) to prevent event loop issues.

Run:
- `poetry run pytest tests/openapi31/test_integration.py -v` -- all tests pass
- `poetry run pytest tests/ -x -q` -- full suite passes
- `poetry run flake8 tests/openapi31/test_integration.py tests/openapi31/conftest.py` -- zero warnings
  </action>
  <verify>
`poetry run pytest tests/openapi31/test_integration.py -v` passes with 15+ tests, 0 failures.
`poetry run pytest tests/ -x -q` passes with 890+ tests total, 0 failures.
`poetry run flake8 tests/openapi31/test_integration.py tests/openapi31/conftest.py` -- zero output.
  </verify>
  <done>Integration test exercises every OAS 3.1 feature through HTTP round-trips. All kitchen-sink endpoints tested with valid and invalid inputs. Form data composition tested (TEST-05). All features combined in one spec that loads and validates (TEST-07). Zero regression in existing tests.</done>
</task>

</tasks>

<verification>
1. `poetry run pytest tests/openapi31/ -v` -- all tests pass (75 consolidated + 15+ integration)
2. `poetry run pytest tests/ -x -q` -- 890+ tests pass, 0 failures
3. `poetry run flake8 tests/openapi31/ tests/fakeapi/openapi31_handlers.py` -- zero output
4. The kitchen-sink YAML spec includes: openapi 3.1.0, jsonSchemaDialect, webhooks, pathItems, mutualTLS, type arrays, const, exclusiveMinimum, $ref siblings, allOf/anyOf/oneOf, form data composition
5. Every endpoint in the kitchen-sink spec has a corresponding test function with HTTP round-trip
</verification>

<success_criteria>
- TEST-04: Every 3.1 feature has dedicated test coverage via kitchen-sink integration
- TEST-05: allOf/anyOf/oneOf form data has dedicated test fixtures and tests
- TEST-07: 3.1 spec with all features combined loads and validates successfully
- TEST-02/TEST-03: Zero regression (full suite passes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-integration/04-02-SUMMARY.md`
</output>
