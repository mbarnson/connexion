---
phase: 04-testing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - connexion/spec.py
  - connexion/operations/openapi31.py
  - tests/openapi31/__init__.py
  - tests/openapi31/test_spec_detection.py
  - tests/openapi31/test_validation.py
  - tests/openapi31/test_features.py
autonomous: true

must_haves:
  truths:
    - "F401 unused import in openapi31.py is fixed"
    - "isort formatting in spec.py is fixed"
    - "All Phase 1-3 tests exist in tests/openapi31/ directory"
    - "Original test files (test_spec_31.py, test_validation_31.py, test_oas31_features.py) are removed"
    - "All 75 Phase 1-3 tests pass from new location"
    - "All 877+ existing tests pass with zero regressions"
  artifacts:
    - path: "tests/openapi31/__init__.py"
      provides: "Python package marker for openapi31 test directory"
    - path: "tests/openapi31/test_spec_detection.py"
      provides: "Consolidated spec detection tests (from test_spec_31.py)"
      min_lines: 200
    - path: "tests/openapi31/test_validation.py"
      provides: "Consolidated validation tests (from test_validation_31.py)"
      min_lines: 350
    - path: "tests/openapi31/test_features.py"
      provides: "Consolidated feature tests (from test_oas31_features.py)"
      min_lines: 450
  key_links:
    - from: "tests/openapi31/test_spec_detection.py"
      to: "connexion/spec.py"
      via: "imports OpenAPI31Specification, Specification.from_dict"
      pattern: "from connexion"
    - from: "tests/openapi31/test_validation.py"
      to: "connexion/json_schema.py"
      via: "imports validators and resolve_refs"
      pattern: "from connexion"
    - from: "tests/openapi31/test_features.py"
      to: "connexion/operations/openapi31.py"
      via: "imports OpenAPI31Operation"
      pattern: "from connexion"
---

<objective>
Fix lint issues in Phase 1-3 source files and consolidate all 3.1 tests into tests/openapi31/ directory.

Purpose: Clean code quality (TEST-06) and organized test structure as foundation for the kitchen-sink integration test. Consolidation follows connexion's existing "group by feature" pattern (tests/api/) while keeping legacy tests unchanged.

Output: Lint-clean source files, consolidated test directory with all 75 tests passing, zero regression in existing 877+ test suite.
</objective>

<execution_context>
@/Users/patbarnson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patbarnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-integration/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lint issues in Phase 1-3 source files</name>
  <files>connexion/operations/openapi31.py, connexion/spec.py</files>
  <action>
Fix two lint issues identified in Phase 1-3 source files:

1. **F401 in connexion/operations/openapi31.py:** Remove the unused `import typing as t` on line 7. Check that no code in the file uses `t.` before removing.

2. **isort in connexion/spec.py:** Fix the import formatting on line 26. The import `from .operations import AbstractOperation, OpenAPI31Operation, OpenAPIOperation, Swagger2Operation` needs to be reformatted to multi-line style with parentheses (isort black profile). Run `poetry run isort connexion/spec.py` to auto-fix.

After fixing both:
- Run `poetry run flake8 connexion/operations/openapi31.py connexion/spec.py` -- expect zero output
- Run `poetry run isort --check-only connexion/spec.py connexion/operations/openapi31.py` -- expect zero output
- Run `poetry run pytest tests/ -x -q` -- expect all tests pass (zero regression from lint fixes)

Do NOT touch any other files. Only fix files we modified in Phases 1-3.
  </action>
  <verify>
`poetry run flake8 connexion/operations/openapi31.py connexion/spec.py` produces no output.
`poetry run isort --check-only connexion/spec.py connexion/operations/openapi31.py` produces no output.
`poetry run pytest tests/ -x -q` passes with 877+ tests, 0 failures.
  </verify>
  <done>Both lint issues fixed. flake8 and isort produce zero warnings on all Phase 1-3 modified source files.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Phase 1-3 tests into tests/openapi31/ directory</name>
  <files>tests/openapi31/__init__.py, tests/openapi31/test_spec_detection.py, tests/openapi31/test_validation.py, tests/openapi31/test_features.py</files>
  <action>
Consolidate all Phase 1-3 test files into a new `tests/openapi31/` directory. Follow the copy-verify-remove pattern for safety.

**Step 1: Create directory and copy files**
- Create `tests/openapi31/` directory
- Create `tests/openapi31/__init__.py` (empty file)
- Copy `tests/test_spec_31.py` to `tests/openapi31/test_spec_detection.py`
- Copy `tests/test_validation_31.py` to `tests/openapi31/test_validation.py`
- Copy `tests/test_oas31_features.py` to `tests/openapi31/test_features.py`

**Step 2: Fix imports in copied files**
The copied files may use relative imports or imports from conftest that need adjustment. Check each file:
- If they `from conftest import ...`, this should still work because pytest adds tests/ to sys.path
- If they import from each other, update paths
- The key is: `poetry run pytest tests/openapi31/ -v` must find and run all tests

**Step 3: Verify copied tests pass**
- Run `poetry run pytest tests/openapi31/ -v` -- all 75 tests must pass
- Run `poetry run pytest tests/ -x -q` -- full suite must pass (both old and new locations exist temporarily)

**Step 4: Remove original files**
Only after Step 3 passes:
- Remove `tests/test_spec_31.py`
- Remove `tests/test_validation_31.py`
- Remove `tests/test_oas31_features.py`

**Step 5: Final verification**
- Run `poetry run pytest tests/ -x -q` -- full suite must pass with EXACTLY the same test count (not double-counted)
- Run `poetry run pytest tests/openapi31/ -v` -- all 75 tests must pass

Important: Do NOT modify the test content itself. Only move files and fix imports if needed.
  </action>
  <verify>
`poetry run pytest tests/openapi31/ -v` passes with 75 tests, 0 failures.
`poetry run pytest tests/ -x -q` passes with 877+ tests total, 0 failures.
Original test files (test_spec_31.py, test_validation_31.py, test_oas31_features.py) no longer exist in tests/ root.
  </verify>
  <done>All 75 Phase 1-3 tests consolidated into tests/openapi31/ with three files (test_spec_detection.py, test_validation.py, test_features.py). Full test suite passes with zero regressions. Original files removed.</done>
</task>

</tasks>

<verification>
1. `poetry run flake8 connexion/operations/openapi31.py connexion/spec.py` -- zero output
2. `poetry run isort --check-only connexion/spec.py connexion/operations/openapi31.py` -- zero output
3. `poetry run pytest tests/openapi31/ -v` -- 75 tests pass
4. `poetry run pytest tests/ -x -q` -- 877+ tests pass, 0 failures
5. `ls tests/test_spec_31.py tests/test_validation_31.py tests/test_oas31_features.py` -- files not found (removed)
6. `ls tests/openapi31/` -- shows __init__.py, test_spec_detection.py, test_validation.py, test_features.py
</verification>

<success_criteria>
- TEST-06 partial: flake8 and isort clean on all Phase 1-3 modified files
- TEST-04 partial: All 75 existing 3.1 tests organized in consistent structure
- TEST-02/TEST-03: Zero regression in existing Swagger 2.0 and OpenAPI 3.0 tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-integration/04-01-SUMMARY.md`
</output>
