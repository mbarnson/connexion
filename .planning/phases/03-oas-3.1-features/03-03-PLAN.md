---
phase: 03-oas-3.1-features
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/test_oas31_features.py
autonomous: true

must_haves:
  truths:
    - "Webhook parsing and API exposure has test coverage"
    - "pathItems component initialization and $ref resolution has test coverage"
    - "Minimal document loading (no paths) has test coverage"
    - "jsonSchemaDialect recognition and property access has test coverage"
    - "mutualTLS security scheme recognition has test coverage"
    - "$ref sibling preservation has test coverage (verifying Phase 2 work)"
    - "OpenAPI31Operation carries json_schema_dialect from spec"
  artifacts:
    - path: "tests/test_oas31_features.py"
      provides: "Comprehensive tests for all Phase 3 FEAT-* and SPEC-03 requirements"
      min_lines: 150
  key_links:
    - from: "tests/test_oas31_features.py"
      to: "connexion/spec.py (OpenAPI31Specification)"
      via: "direct instantiation and property assertions"
      pattern: "OpenAPI31Specification"
    - from: "tests/test_oas31_features.py"
      to: "connexion/operations/openapi31.py (OpenAPI31Operation)"
      via: "class attribute and inheritance assertions"
      pattern: "OpenAPI31Operation"
    - from: "tests/test_oas31_features.py"
      to: "connexion/security.py (SecurityHandlerFactory)"
      via: "parse_security_scheme call assertions"
      pattern: "parse_security_scheme"
---

<objective>
Create a comprehensive test suite covering all Phase 3 OAS 3.1 features: webhooks, pathItems, minimal documents, jsonSchemaDialect, mutualTLS, $ref siblings, and OpenAPI31Operation.

Purpose: Proves all seven Phase 3 requirements (FEAT-01 through FEAT-06, SPEC-03) are correctly implemented. Each feature gets dedicated test coverage with inline spec fixtures following project conventions (from 01-02 decision).

Output: New test file `tests/test_oas31_features.py` with comprehensive coverage.
</objective>

<execution_context>
@/Users/patbarnson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patbarnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-oas-3.1-features/03-RESEARCH.md
@.planning/phases/03-oas-3.1-features/03-01-SUMMARY.md
@.planning/phases/03-oas-3.1-features/03-02-SUMMARY.md
@connexion/spec.py
@connexion/operations/openapi31.py
@connexion/security.py
@tests/test_spec_31.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 3 feature test suite</name>
  <files>tests/test_oas31_features.py</files>
  <action>
Create `tests/test_oas31_features.py` with inline spec fixtures and comprehensive test coverage. Follow project conventions: inline specs, parametrized where appropriate, explicit requirement mapping in test names/docstrings.

The test file should cover these requirement groups:

**FEAT-01: Webhooks**
- `test_webhooks_parsed_and_accessible`: Spec with webhooks key, assert `spec.webhooks` returns dict with webhook name as key
- `test_webhooks_empty_when_not_defined`: Spec without webhooks, assert `spec.webhooks == {}`
- `test_webhooks_path_item_structure`: Verify webhook value has expected Path Item Object structure (methods, responses)

**FEAT-02: pathItems in components**
- `test_path_items_in_components`: Spec with `components/pathItems`, assert accessible via `spec.components["pathItems"]`
- `test_path_items_default_empty`: Spec without pathItems, assert `spec.components["pathItems"] == {}`
- `test_path_items_ref_in_paths`: Spec with `paths: { "/health": { "$ref": "#/components/pathItems/HealthCheck" } }` and corresponding component -- assert the path resolves correctly after ref resolution

**FEAT-03: Minimal documents**
- `test_minimal_document_webhooks_only`: Spec with only `openapi`, `info`, and `webhooks` (no `paths`) loads without error
- `test_minimal_document_components_only`: Spec with only `openapi`, `info`, and `components` (no `paths`) loads without error
- `test_minimal_document_has_empty_paths`: Minimal doc has `spec.get("paths") == {}`

**FEAT-04: jsonSchemaDialect**
- `test_json_schema_dialect_present`: Spec with `jsonSchemaDialect` key, assert `spec.json_schema_dialect` returns the URI
- `test_json_schema_dialect_absent`: Spec without key, assert `spec.json_schema_dialect is None`
- `test_json_schema_dialect_default_2020_12`: Spec with explicit Draft 2020-12 URI, assert it returns correctly

**FEAT-05: mutualTLS**
- `test_mutual_tls_recognized`: `SecurityHandlerFactory().parse_security_scheme({"type": "mutualTLS"}, [])` returns `security_passthrough`
- `test_mutual_tls_not_none`: Result is not None (prevents "unsupported" warning path)
- `test_mutual_tls_does_not_break_other_types`: After mutualTLS addition, apiKey/http/oauth2 types still work (parametrized)

**FEAT-06: $ref siblings (verification of Phase 2)**
- `test_ref_siblings_preserved_in_31`: Spec with `$ref` alongside `summary` and `description`, assert both are preserved after spec resolution
- `test_ref_siblings_not_preserved_in_30`: Same pattern in a 3.0 spec, assert legacy behavior (siblings stripped)

**SPEC-03: OpenAPI31Operation**
- `test_operation_cls_is_openapi31`: `OpenAPI31Specification.operation_cls is OpenAPI31Operation`
- `test_openapi31_operation_inherits_openapi`: `issubclass(OpenAPI31Operation, OpenAPIOperation)`
- `test_openapi31_operation_has_json_schema_dialect`: Construct operation with `json_schema_dialect` param, assert property returns it
- `test_openapi31_operation_default_dialect_none`: Construct without param, assert property is None

Use helper functions to create base specs to reduce repetition:

```python
def make_31_spec(**overrides):
    """Create a minimal valid OAS 3.1 spec with optional overrides."""
    spec = {
        "openapi": "3.1.0",
        "info": {"title": "Test", "version": "1.0.0"},
    }
    spec.update(overrides)
    return spec
```

Organize tests into classes by requirement:
- `class TestWebhooks` (FEAT-01)
- `class TestPathItems` (FEAT-02)
- `class TestMinimalDocuments` (FEAT-03)
- `class TestJsonSchemaDialect` (FEAT-04)
- `class TestMutualTLS` (FEAT-05)
- `class TestRefSiblings` (FEAT-06)
- `class TestOpenAPI31Operation` (SPEC-03)

Import from:
- `connexion.spec` (OpenAPI31Specification, OpenAPISpecification)
- `connexion.operations.openapi31` (OpenAPI31Operation)
- `connexion.operations.openapi` (OpenAPIOperation)
- `connexion.security` (SecurityHandlerFactory)
  </action>
  <verify>
Run: `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/test_oas31_features.py -v --tb=short` -- all tests pass.
  </verify>
  <done>All Phase 3 features have dedicated test coverage with inline specs, organized by requirement, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Run full regression test suite</name>
  <files></files>
  <action>
Run the full test suite to confirm zero regression:

1. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/test_spec_31.py tests/test_validation_31.py tests/test_oas31_features.py -v --tb=short` -- all 3.1 tests pass
2. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/ -x -q --ignore=tests/test_spec_31.py --ignore=tests/test_validation_31.py --ignore=tests/test_oas31_features.py` -- all existing tests pass (or skip expected failures)

If any tests fail that are NOT pre-existing failures, investigate and fix. Do NOT modify existing test files -- fix the source code if there's a real regression.

Report exact pass/fail counts.
  </action>
  <verify>All test suites pass with zero new failures. Report test counts.</verify>
  <done>Full regression suite passes with zero new failures introduced by Phase 3</done>
</task>

</tasks>

<verification>
1. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/test_oas31_features.py -v` -- all feature tests pass
2. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/test_spec_31.py -v` -- Phase 1 tests still pass
3. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/test_validation_31.py -v` -- Phase 2 tests still pass
4. `cd /Users/patbarnson/devel/disposable_music/connexion && python3 -m pytest tests/ -x -q` -- full suite passes
</verification>

<success_criteria>
- tests/test_oas31_features.py exists with tests for all 7 requirements (FEAT-01 through FEAT-06, SPEC-03)
- All new tests pass
- All existing tests pass (zero regression)
- Test organization follows project conventions (inline specs, classes by requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/03-oas-3.1-features/03-03-SUMMARY.md`
</output>
