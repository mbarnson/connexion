---
phase: 01-spec-detection-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_spec_31.py
autonomous: true

must_haves:
  truths:
    - "Tests prove 3.1.0 spec routes to OpenAPI31Specification"
    - "Tests prove 3.1.1 and 3.1.2 patch versions route correctly"
    - "Tests prove 3.0.x specs still route to OpenAPISpecification"
    - "Tests prove 2.0 specs still route to Swagger2Specification"
    - "Tests prove 4.0.0 raises InvalidSpecification with supported versions listed"
    - "Tests prove validation errors include OAS version number"
    - "Tests prove the bundled v3.1 schema is valid and loadable"
    - "Tests prove a valid minimal 3.1 spec validates without errors"
    - "Tests prove an invalid 3.1 spec (e.g. missing info) raises InvalidSpecification"
  artifacts:
    - path: "tests/test_spec_31.py"
      provides: "Comprehensive test suite for Phase 1 spec detection and validation"
      min_lines: 80
  key_links:
    - from: "tests/test_spec_31.py"
      to: "connexion/spec.py"
      via: "imports Specification, OpenAPI31Specification, InvalidSpecification"
      pattern: "from connexion\\.spec import"
---

<objective>
Create a comprehensive test suite proving all Phase 1 success criteria are met: version detection, routing, meta-schema validation, error messages, and zero regression.

Purpose: Without tests, we cannot prove the implementation works or detect regressions. These tests serve as the verification gate for Phase 1 completion.

Output: `tests/test_spec_31.py` with focused tests covering every Phase 1 requirement (SPEC-01, SPEC-02, SPEC-04, SPEC-05).
</objective>

<execution_context>
@/Users/patbarnson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patbarnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-spec-detection-schema/01-RESEARCH.md
@.planning/phases/01-spec-detection-schema/01-CONTEXT.md
@.planning/phases/01-spec-detection-schema/01-01-SUMMARY.md

@connexion/spec.py
@tests/conftest.py
@tests/test_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenAPI 3.1 spec detection and validation test suite</name>
  <files>tests/test_spec_31.py</files>
  <action>
Create `tests/test_spec_31.py` with the following test cases. Each test maps to a Phase 1 requirement.

**Test structure:**

```python
"""Tests for OpenAPI 3.1 spec detection, routing, and validation (Phase 1).

Covers requirements: SPEC-01, SPEC-02, SPEC-04, SPEC-05
"""
import json
import pkgutil

import pytest
from connexion.exceptions import InvalidSpecification
from connexion.spec import (
    OpenAPI31Specification,
    OpenAPISpecification,
    Specification,
    Swagger2Specification,
)
```

**Helper fixtures:**
- `valid_31_spec()` - Returns a minimal valid OpenAPI 3.1.0 spec dict (openapi, info with title+version, paths)
- `valid_30_spec()` - Returns a minimal valid OpenAPI 3.0.0 spec dict
- `valid_20_spec()` - Returns a minimal valid Swagger 2.0 spec dict

**Test cases for SPEC-01 (version detection routes to OpenAPI31Specification):**

1. `test_31_spec_routes_to_openapi31_specification` - `from_dict()` with `openapi: "3.1.0"` returns `OpenAPI31Specification`
2. `test_31_patch_versions_route_correctly` - Parametrize with "3.1.0", "3.1.1", "3.1.2" -- all return `OpenAPI31Specification`

**Test cases for SPEC-05 (three-way version branching):**

3. `test_30_spec_routes_to_openapi_specification` - `from_dict()` with `openapi: "3.0.0"` returns `OpenAPISpecification`
4. `test_30_patch_versions_route_correctly` - Parametrize with "3.0.0", "3.0.1", "3.0.3" -- all return `OpenAPISpecification`
5. `test_20_spec_routes_to_swagger2_specification` - `from_dict()` with `swagger: "2.0"` returns `Swagger2Specification`
6. `test_unsupported_version_raises_error` - `from_dict()` with `openapi: "4.0.0"` raises `InvalidSpecification` with message containing "not supported" and "Supported versions: 2.0, 3.0.x, 3.1.x"
7. `test_missing_version_raises_error` - `from_dict()` with `{}` raises `InvalidSpecification` with message containing "3.1.x"
8. `test_invalid_version_string_raises_error` - `from_dict()` with `openapi: "not.a.version"` raises `InvalidSpecification`

**Test cases for SPEC-02 (meta-schema validates valid specs):**

9. `test_valid_31_spec_validates_successfully` - A valid minimal 3.1 spec passes validation (no exception)
10. `test_invalid_31_spec_missing_info_fails` - A 3.1 spec without `info` raises `InvalidSpecification`
11. `test_invalid_31_spec_missing_openapi_field_fails` - A 3.1 spec without `openapi` field raises error
12. `test_bundled_schema_is_valid_json` - `pkgutil.get_data("connexion", "resources/schemas/v3.1/schema.json")` loads and parses as JSON
13. `test_bundled_schema_uses_draft_2020_12` - The loaded schema's `$schema` field contains "draft/2020-12"
14. `test_bundled_schema_has_31_version_pattern` - The schema's openapi property pattern matches 3.1.x versions

**Test cases for SPEC-04 (error messages reference 3.1.0):**

15. `test_validation_error_includes_version` - When a 3.1 spec fails validation, the error message contains "3.1.0" (not "3.0.0")
16. `test_unsupported_version_error_message_format` - Error for "4.0.0" contains "4.0.0" and "Supported versions"

**Regression test cases:**

17. `test_existing_30_spec_still_validates` - A known-good 3.0 spec still validates correctly
18. `test_existing_20_spec_still_validates` - A known-good 2.0 spec still validates correctly

**Implementation notes:**
- Use `pytest.fixture` for spec dict helpers
- Use `@pytest.mark.parametrize` for version lists
- Keep test names descriptive and requirement-traceable
- For fixtures, create minimal valid specs inline (not YAML files) -- this keeps tests self-contained
- For the valid 3.1 spec, the minimal structure is: `{"openapi": "3.1.0", "info": {"title": "Test", "version": "1.0"}, "paths": {}}`
- For the valid 3.0 spec: `{"openapi": "3.0.0", "info": {"title": "Test", "version": "0.1.0"}, "paths": {}}`
- For the valid 2.0 spec: `{"swagger": "2.0", "info": {"title": "Test", "version": "0.1.0"}, "paths": {}, "basePath": "/"}`
  </action>
  <verify>
Run the new test file:
`python -m pytest tests/test_spec_31.py -v --timeout=60`

All tests should pass. Then run the full suite to verify no regression:
`python -m pytest tests/ -x -q --timeout=60 2>&1 | tail -5`
  </verify>
  <done>
`tests/test_spec_31.py` exists with 15+ test cases covering all four Phase 1 requirements (SPEC-01, SPEC-02, SPEC-04, SPEC-05). All tests pass. Full test suite passes with zero regression.
  </done>
</task>

</tasks>

<verification>
1. New tests pass: `python -m pytest tests/test_spec_31.py -v`
2. Full regression: `python -m pytest tests/ -x -q --timeout=60`
3. Code quality: `python -m flake8 tests/test_spec_31.py`
</verification>

<success_criteria>
- `tests/test_spec_31.py` exists with comprehensive test coverage
- All new tests pass
- Every Phase 1 requirement (SPEC-01, SPEC-02, SPEC-04, SPEC-05) has at least one test
- Regression tests prove 2.0 and 3.0 behavior is unchanged
- Meta-schema integrity tests prove the bundled schema is correct
- Full test suite passes with zero regression
</success_criteria>

<output>
After completion, create `.planning/phases/01-spec-detection-schema/01-02-SUMMARY.md`
</output>
