---
phase: 02-json-schema-2020-12-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - connexion/validators/json.py
  - connexion/validators/parameter.py
  - connexion/validators/form_data.py
  - connexion/middleware/request_validation.py
  - connexion/middleware/response_validation.py
autonomous: true

must_haves:
  truths:
    - "Request bodies in 3.1 specs validate using Draft202012Validator, not Draft4"
    - "Response bodies in 3.1 specs validate using Draft202012Validator, not Draft4"
    - "type: ['string', 'null'] in a 3.1 schema correctly accepts both string and null values"
    - "exclusiveMinimum: 5 in a 3.1 schema validates as a numeric bound, not a boolean"
    - "const keyword in a 3.1 schema validates correctly"
    - "unevaluatedProperties in a 3.1 schema validates correctly"
    - "Parameter validation in 3.1 specs uses Draft202012Validator"
    - "Form data with allOf/anyOf/oneOf composition validates correctly in 3.1 specs"
    - "3.0 and 2.0 specs continue using Draft4Validator with zero regression"
  artifacts:
    - path: "connexion/validators/json.py"
      provides: "Version-aware JSON request and response body validators"
      contains: "spec_version"
    - path: "connexion/validators/parameter.py"
      provides: "Version-aware parameter validator"
      contains: "spec_version"
    - path: "connexion/validators/form_data.py"
      provides: "Version-aware form data validator"
      contains: "spec_version"
    - path: "connexion/middleware/request_validation.py"
      provides: "Passes spec_version from operation to request validators"
      contains: "spec_version"
    - path: "connexion/middleware/response_validation.py"
      provides: "Passes spec_version from operation to response validators"
      contains: "spec_version"
  key_links:
    - from: "connexion/middleware/request_validation.py"
      to: "connexion/validators/json.py"
      via: "spec_version passed in validator constructor kwargs"
      pattern: "spec_version.*operation.*spec_version"
    - from: "connexion/validators/json.py"
      to: "connexion/json_schema.py"
      via: "imports Draft202012RequestValidator/ResponseValidator"
      pattern: "Draft202012RequestValidator"
    - from: "connexion/middleware/request_validation.py"
      to: "connexion/validators/parameter.py"
      via: "spec_version passed to ParameterValidator constructor"
      pattern: "spec_version"
---

<objective>
Wire spec_version through the middleware layer to all validator classes so that 3.1 specs use Draft 2020-12 validation for request bodies, response bodies, parameters, and form data.

Purpose: This completes the validation engine upgrade. Plan 01 created the Draft 2020-12 validators and plumbing; this plan connects them to the actual validation pipeline. After this plan, all 10 VALID-* requirements are satisfied.

Output: All five validator/middleware files updated to pass and use spec_version for version-conditional validator selection.
</objective>

<execution_context>
@/Users/patbarnson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patbarnson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-json-schema-2020-12-validation/02-CONTEXT.md
@.planning/phases/02-json-schema-2020-12-validation/02-RESEARCH.md
@.planning/phases/02-json-schema-2020-12-validation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire spec_version through middleware to all validators</name>
  <files>connexion/middleware/request_validation.py, connexion/middleware/response_validation.py, connexion/validators/json.py, connexion/validators/parameter.py, connexion/validators/form_data.py</files>
  <action>
**In `connexion/middleware/request_validation.py`:**

1. In `RequestValidationOperation.__call__()`, extract spec_version from the operation and pass it to both the parameter validator and body validators:
   - Before parameter validator instantiation, get: `spec_version = getattr(self._operation, 'spec_version', (3, 0, 0))`
   - Pass to ParameterValidator: add `spec_version=spec_version` to the constructor call (line ~104-108)
   - Pass to body validator: add `spec_version=spec_version` to the validator constructor call (line ~128-139). The body validator is instantiated with kwargs, so add `spec_version=spec_version` alongside the existing `schema=`, `required=`, `nullable=`, `encoding=`, `strict_validation=`, `uri_parser=` arguments

**In `connexion/middleware/response_validation.py`:**

2. In `ResponseValidationOperation.__call__()`, inside the `wrapped_send` function, extract spec_version and pass to response body validator:
   - Get: `spec_version = getattr(self._operation, 'spec_version', (3, 0, 0))`
   - Pass to body_validator constructor: add `spec_version=spec_version` to the validator constructor call (line ~115-122)

**In `connexion/validators/json.py`:**

3. Update imports to include Draft 2020-12 validators:
   ```python
   from connexion.json_schema import (
       Draft4RequestValidator,
       Draft4ResponseValidator,
       Draft202012RequestValidator,
       Draft202012ResponseValidator,
       format_error_with_path,
   )
   ```

4. Update `JSONRequestBodyValidator.__init__()`:
   - Add `spec_version: tuple = (3, 0, 0)` parameter (accept via `**kwargs` or explicit param)
   - Store as `self._spec_version = spec_version`
   - Note: `**kwargs` in the existing signature already passes through, but explicit is better

5. Update `JSONRequestBodyValidator._validator` property:
   - If `self._spec_version >= (3, 1, 0)`: use `Draft202012RequestValidator(self._schema, format_checker=Draft202012Validator.FORMAT_CHECKER)`
   - Else: keep existing `Draft4RequestValidator(self._schema, format_checker=Draft4Validator.FORMAT_CHECKER)`
   - Import `Draft202012Validator` from jsonschema for FORMAT_CHECKER access

6. Update `DefaultsJSONRequestBodyValidator._validator` property similarly:
   - If `self._spec_version >= (3, 1, 0)`: use `self.extend_with_set_default(Draft202012RequestValidator)` with `Draft202012Validator.FORMAT_CHECKER`
   - Else: keep existing behavior

7. Update `JSONResponseBodyValidator.__init__()`:
   - Accept `spec_version: tuple = (3, 0, 0)` parameter (after `scope` and before/in `**kwargs`)
   - Store as `self._spec_version = spec_version`
   - Note: `AbstractResponseBodyValidator.__init__` takes `scope, *, schema, nullable, encoding` — spec_version should be added as keyword param

8. Update `JSONResponseBodyValidator.validator` property:
   - If `self._spec_version >= (3, 1, 0)`: use `Draft202012ResponseValidator(self._schema, format_checker=Draft202012Validator.FORMAT_CHECKER)`
   - Else: keep existing `Draft4ResponseValidator(self._schema, format_checker=Draft4Validator.FORMAT_CHECKER)`

**In `connexion/validators/parameter.py`:**

9. Update `ParameterValidator.__init__()`:
   - Add `spec_version=(3, 0, 0)` parameter
   - Store as `self._spec_version = spec_version`

10. Update `ParameterValidator.validate_parameter()`:
    - Change from `@staticmethod` to instance method (needs self._spec_version)
    - Or: add `spec_version` parameter to validate_parameter and pass from callers
    - Cleaner approach: make it an instance method, remove `@staticmethod`, add `self` parameter
    - If `self._spec_version >= (3, 1, 0)`: use `Draft202012Validator(param, format_checker=Draft202012Validator.FORMAT_CHECKER)`
    - Else: keep existing `Draft4Validator(param, format_checker=draft4_format_checker)`
    - Import `Draft202012Validator` from jsonschema
    - For 3.1 specs, skip the `is_nullable(param) and is_null(value)` early return since 3.1 uses type arrays for nullability (the Draft202012Validator handles this natively)

**In `connexion/validators/form_data.py`:**

11. Update imports:
    ```python
    from connexion.json_schema import Draft4RequestValidator, Draft202012RequestValidator, format_error_with_path
    ```

12. Update `FormDataValidator.__init__()`:
    - Add `spec_version: tuple = (3, 0, 0)` parameter
    - Store as `self._spec_version = spec_version`

13. Update `FormDataValidator._validator` property:
    - If `self._spec_version >= (3, 1, 0)`: use `Draft202012RequestValidator(self._schema, format_checker=Draft202012Validator.FORMAT_CHECKER)`
    - Else: keep existing `Draft4RequestValidator(self._schema, format_checker=Draft4Validator.FORMAT_CHECKER)`
    - Import `Draft202012Validator` from jsonschema for FORMAT_CHECKER
    - allOf/anyOf/oneOf composition works natively in Draft202012Validator — no additional code needed per RESEARCH.md

**Important notes for all changes:**
- Default `spec_version=(3, 0, 0)` everywhere ensures zero regression for existing callers
- The `nullable` parameter in validator constructors remains for 3.0/2.0 compat. For 3.1, type arrays in schema handle nullability, but the constructor param is harmless and backwards-compatible
- For 3.1 specs in ParameterValidator, `is_nullable()` checks `schema.get("nullable", False)` which will be False (since 3.1 doesn't use nullable keyword), so the null check falls through to Draft202012Validator which handles type arrays correctly
  </action>
  <verify>
Run flake8 on all modified files:
```
.venv/bin/python -m flake8 connexion/validators/json.py connexion/validators/parameter.py connexion/validators/form_data.py connexion/middleware/request_validation.py connexion/middleware/response_validation.py
```

Run existing tests to verify zero regression:
```
.venv/bin/python -m pytest tests/ -x -q --timeout=60
```

Run a quick import check:
```
.venv/bin/python -c "
from connexion.validators.json import JSONRequestBodyValidator, JSONResponseBodyValidator
from connexion.validators.parameter import ParameterValidator
from connexion.validators.form_data import FormDataValidator
print('All validators import successfully')
"
```
  </verify>
  <done>
All five files updated with spec_version plumbing. 3.1 specs use Draft202012 validators. 3.0/2.0 specs continue using Draft4 validators. All existing tests pass with zero regression.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 2 validation tests covering all VALID-* requirements</name>
  <files>tests/test_validation_31.py</files>
  <action>
Create `tests/test_validation_31.py` with tests proving all 10 VALID-* requirements work.

Follow the established pattern from `tests/test_spec_31.py` (inline fixtures, parametrized tests, explicit requirement mapping).

**Test structure:**

```python
"""Tests for Phase 2: JSON Schema 2020-12 Validation (VALID-01 through VALID-10)."""
import pytest
from jsonschema import Draft202012Validator, Draft4Validator
```

**VALID-01 / VALID-02: Draft 2020-12 validators for request/response bodies**
- `test_draft202012_request_validator_exists`: Import and verify Draft202012RequestValidator from connexion.json_schema
- `test_draft202012_response_validator_exists`: Import and verify Draft202012ResponseValidator from connexion.json_schema
- `test_request_validator_uses_draft202012_for_31`: Create JSONRequestBodyValidator with spec_version=(3,1,0), verify its _validator is based on Draft202012Validator (check `type(v._validator).__name__` or validator class ancestry)
- `test_request_validator_uses_draft4_for_30`: Create JSONRequestBodyValidator with default spec_version, verify _validator is Draft4RequestValidator
- `test_response_validator_uses_draft202012_for_31`: Similarly for JSONResponseBodyValidator

**VALID-03: Type arrays**
- `test_type_array_accepts_string`: Schema `{"type": ["string", "null"]}` validates "hello" via Draft202012RequestValidator
- `test_type_array_accepts_null`: Same schema validates None
- `test_type_array_rejects_integer`: Same schema rejects 42
- `test_multi_type_array`: Schema `{"type": ["string", "integer", "null"]}` validates "hello", 42, and None

**VALID-04: Strict nullable rejection**
- `test_nullable_in_31_spec_rejected_at_load`: Create a 3.1 spec with nullable: true in a schema, verify OpenAPI31Specification raises InvalidSpecification
- `test_nullable_error_is_actionable`: Verify the error message contains guidance about using type arrays (e.g., contains "type" and "null")
- `test_nullable_in_30_spec_still_works`: Create a 3.0 spec with nullable: true, verify it loads without error

**VALID-05: Numeric exclusive bounds**
- `test_exclusive_minimum_numeric_passes`: Schema `{"type": "integer", "exclusiveMinimum": 5}` validates 6 via Draft202012Validator
- `test_exclusive_minimum_numeric_rejects`: Same schema rejects 5 (boundary) and 4
- `test_exclusive_maximum_numeric`: Schema `{"type": "integer", "exclusiveMaximum": 10}` validates 9, rejects 10

**VALID-06: const keyword**
- `test_const_validates_matching_value`: Schema `{"const": "active"}` validates "active"
- `test_const_rejects_non_matching_value`: Same schema rejects "inactive"

**VALID-07: $ref with sibling preservation**
- `test_resolve_refs_preserves_siblings_for_31`: Create a spec dict with a $ref that has sibling keywords (e.g., `{"$ref": "#/components/schemas/Foo", "description": "override"}`), call resolve_refs with spec_version=(3,1,0), verify description is preserved in resolved output
- `test_resolve_refs_strips_siblings_for_30`: Same spec, call resolve_refs with spec_version=(3,0,0), verify existing behavior (siblings may be overwritten)

**VALID-08: Parameter validation**
- `test_parameter_validator_uses_draft202012_for_31`: Create ParameterValidator with spec_version=(3,1,0), verify it uses Draft202012Validator for validation (test by validating a type array parameter)
- `test_parameter_validator_uses_draft4_for_30`: Create ParameterValidator with default spec_version, verify Draft4Validator behavior

**VALID-09: Form data composition**
- `test_form_data_validator_uses_draft202012_for_31`: Create FormDataValidator with spec_version=(3,1,0), verify _validator is Draft 2020-12 based
- `test_form_data_allof_composition_31`: Schema with allOf composition validates correctly with Draft202012Validator (test the validator directly, not through ASGI pipeline)

**VALID-10: unevaluatedProperties**
- `test_unevaluated_properties_rejects_extra`: Schema with `"unevaluatedProperties": false` and allOf composition rejects extra properties via Draft202012Validator
- `test_unevaluated_properties_accepts_declared`: Same schema accepts properties declared in allOf branches

**Regression tests:**
- `test_30_request_body_validation_unchanged`: Verify a 3.0-style schema with nullable: true still validates correctly
- `test_20_spec_validation_unchanged`: Verify swagger 2.0 specs still work

Each test should be standalone and fast. Use Draft202012Validator and Draft4Validator directly for keyword-level tests (VALID-03, 05, 06, 10). Use the connexion validator classes for integration-level tests (VALID-01, 02, 08, 09).
  </action>
  <verify>
Run the new test file:
```
.venv/bin/python -m pytest tests/test_validation_31.py -v --timeout=60
```

Run flake8:
```
.venv/bin/python -m flake8 tests/test_validation_31.py
```

Run all tests to confirm zero regression:
```
.venv/bin/python -m pytest tests/ -x -q --timeout=60
```
  </verify>
  <done>
All VALID-01 through VALID-10 requirements have dedicated test coverage. All tests pass. Zero regression in existing test suite.
  </done>
</task>

</tasks>

<verification>
1. All VALID-* requirements tested: `.venv/bin/python -m pytest tests/test_validation_31.py -v --timeout=60`
2. Zero regression: `.venv/bin/python -m pytest tests/ -x -q --timeout=60`
3. Clean code: `.venv/bin/python -m flake8 connexion/validators/ connexion/middleware/request_validation.py connexion/middleware/response_validation.py tests/test_validation_31.py`
4. Draft 2020-12 validators used for 3.1 specs (verified by tests)
5. Draft 4 validators still used for 3.0/2.0 specs (verified by regression tests)
</verification>

<success_criteria>
- JSONRequestBodyValidator uses Draft202012RequestValidator when spec_version >= (3, 1, 0)
- JSONResponseBodyValidator uses Draft202012ResponseValidator when spec_version >= (3, 1, 0)
- ParameterValidator uses Draft202012Validator when spec_version >= (3, 1, 0)
- FormDataValidator uses Draft202012RequestValidator when spec_version >= (3, 1, 0)
- Type arrays, const, exclusiveMinimum/Maximum, unevaluatedProperties all validate correctly
- allOf/anyOf/oneOf in form data validates correctly
- All 3.0/2.0 validation paths unchanged
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-json-schema-2020-12-validation/02-02-SUMMARY.md`
</output>
